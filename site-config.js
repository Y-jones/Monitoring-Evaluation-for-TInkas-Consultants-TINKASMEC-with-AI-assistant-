// ============================================================
// TinkasMec site config — the ONE place to update your backend URL.
// ============================================================
// While testing locally with `npm start` in the server folder, leave this as-is.
// Once you deploy the backend (see server/README.md), change this single line
// to your deployed URL, e.g.:
//   window.TINKASMEC_API_BASE = 'https://tinkasmec-backend.onrender.com';
// Both the chat widget and the contact form read from this one value.

window.TINKASMEC_API_BASE = 'http://localhost:3000';